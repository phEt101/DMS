{
  "name": "boswell-dms-api",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "start": "node dist/src/server.js",
    "db:migrate": "sh -c 'if [ \"$NODE_ENV\" = \"production\" ]; then node dist/scripts/migrate.js; else tsx scripts/migrate.ts; fi'",
    "db:seed": "sh -c 'if [ \"$NODE_ENV\" = \"production\" ]; then node dist/scripts/seed.js; else tsx scripts/seed.ts; fi'",
    "db:fresh": "sh -c 'if [ \"$NODE_ENV\" = \"production\" ]; then node dist/scripts/fresh.js; else tsx scripts/fresh.ts; fi'",
    "db:fresh:seed": "sh -c 'if [ \"$NODE_ENV\" = \"production\" ]; then node dist/scripts/fresh.js --seed; else tsx scripts/fresh.ts --seed; fi'"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cookie-parser": "^1.4.7",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "express-rate-limit": "^8.7.0",
    "helmet": "^8.1.0",
    "mysql2": "^3.14.3",
    "react-icons": "^5.7.0"
  },
  "devDependencies": {
    "@types/cookie-parser": "^1.4.10",
    "@types/express": "^5.0.6",
    "@types/node": "^22.20.1",
    "tsx": "^4.23.12",
    "typescript": "^5.9.3"
  }
}
