# Assets

Place static visual files here, such as:

- `doc-icon.svg`
- logo images
- background illustrations
- exported UI icons

Use this folder for files that are imported directly by the app, or move shared public files to `public/` if they should be served as-is.
